import Layout from "../Layout";
/* import { gameManager } from '../../../../../modules/games/garbige/GarbigeGame' */
/* import { GameEvents } from "../../../../../../../application/modules/garbige-game/manager/event-emitter/GameEvents"; */

export default class StartLayout extends Layout {
  /* constructor(tagId: string) {
    super(tagId);
    this.subscribe();
  } */

  private subscribe(): void {
    /* gameManager.gameEventEmitter.subscribe(GameEvents.Start, () => this.hide()); */
  }
}